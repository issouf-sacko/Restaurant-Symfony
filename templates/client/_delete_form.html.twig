<form method="post" action="{{ path('client_delete', {'idclient': client.idclient}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.idclient) }}">
    <button class="btn">Delete</button>
</form>
